export enum UserStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
}

export enum UserRole {
  USER = "user",
  ADMIN = "admin",
  AUTHOR = "author",
}
export enum UserApprovalStatus {
  PENDING = "pending",
  APPROVED = "approved",
  REJECTED = "rejected",
}
